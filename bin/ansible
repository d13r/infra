#!/usr/bin/env bash
set -euo pipefail

export ANSIBLE_VAULT_PASSWORD_FILE
ANSIBLE_VAULT_PASSWORD_FILE="$(dirname "$0")/../libexec/vault-password"

exec ansible -i "$(dirname "$0")/../ansible/inventory.ini" "$@"
