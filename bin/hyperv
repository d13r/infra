#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/../hyper-v"

script=$(wslpath -aw setup.ps1)

exec powershell.exe -Command - <<END
Start-Process powershell -ArgumentList '-ExecutionPolicy Bypass -File "$script"' -Verb RunAs
END
